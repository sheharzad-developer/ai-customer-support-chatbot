#! backend-uploads-persistentVolumeClaim.yaml
# Generated code, do not edit
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
    name: backend-uploads
    namespace: ai-customer-support-chatbot-rag
    labels:
        com.docker.compose.service: backend
        com.docker.compose.volume: uploads
spec:
    accessModes:
        # TODO would need to check which services use this volume
        - ReadWriteOnce
    volumeMode: Filesystem
    resources:
        requests:
            storage: 100Mi
